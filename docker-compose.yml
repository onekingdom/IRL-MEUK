services:
  srt-relay:
    build: .
    restart: unless-stopped
    environment:
      - LISTEN_PORT=8000
      - OBS_PORT=8001
    ports:
      - "8000:8000/udp"
      - "8001:8001/udp"
    volumes:
      - ./data:/app/data
